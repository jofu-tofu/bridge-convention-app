# Project Skills

This repository keeps project-specific skills in `skills/` so their source of truth ships with the codebase.

Current repo-local skills:

- `BridgeExpertReview`
- `ConventionForge`

Use `bash scripts/link-project-skills.sh` to symlink these into local tool directories such as `.claude/skills`, `.codex/skills`, and `.devin/skills`.
