// constraint-utils.ts — previously held cleanConstraints() for stripping
// non-serializable fields before WASM serialization. All deal generation
// now runs in Rust; this utility was only exercised by tests. Removed as dead code.
