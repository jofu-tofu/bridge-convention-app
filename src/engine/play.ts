// play.ts — Trick play rules.
// All runtime play logic lives in Rust (bridge-engine). This file previously
// held TS reference implementations (getLeadSuit, getLegalPlays, getTrickWinner)
// that were only exercised by tests. Removed as dead code after full Rust migration.
