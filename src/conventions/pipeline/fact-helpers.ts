// Re-export from canonical location in conventions/core/fact-catalog.
// Helpers were merged into fact-catalog from the former fact-helpers.ts.
export { num, bool, fv } from "../core/fact-catalog";
export type { HandFactResolverFn } from "../core/fact-catalog";
