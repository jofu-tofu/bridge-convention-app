/**
 * ConversationFlowTree companion — layout is CSS flexbox-driven.
 *
 * This file is intentionally minimal. The tree uses recursive Svelte
 * snippets with CSS pseudo-element connectors instead of computed
 * SVG positions. Kept as a companion stub per codebase convention.
 */
