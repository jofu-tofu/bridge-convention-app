# Bergen Raises — Deferred Surface Work

- Relax Bergen support surfaces from `eq 4` to canonical `gte 4`.
- Rework the direct 4M jump so it is preemptive with 5+ trumps instead of the current game-strength treatment.
- Decide whether to author Reverse Bergen as a separate partnership variant or as system-configured surfaces.
- If a dedicated Jacoby 2NT major-raise module is added later, add the Bergen reference-page cross-link then.
