# Michaels / Unusual 2NT — Deferred Surface Work

- Split weak-Michaels and strong-Michaels into separate or system-configured surface sets instead of one blended teaching surface.
- Add a typed `overcall.twoSuitedShape` partition so the reference page can regain opening-by-opening detail without prose fallback.
- Check whether the derived summary-card bid still renders as `2♣` from the defining meaning; if so, the family-of-bids summary needs a framework-level derivation fix rather than a fixture-only patch.
