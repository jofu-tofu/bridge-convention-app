# Weak Twos — Deferred Surface Work

- Add vulnerability-sensitive opening ranges once the catalog can express `vulnerability`.
- The current authored summary card cannot override `guidingIdea`; the viewport composes it from `teaching.principle`, so the desired suit-quality sentence needs either renderer support or a separate pass with permission to edit `teaching.principle`.

## Resolved (ConventionForge Verify 2026-04-15)

- ~~Lower the new-suit-forcing continuation threshold from the current `16+` surface clauses toward the canonical `14+` treatment.~~ Fixture now authors `hand.hcp >= 14` on the NSF surfaces.
- ~~Revisit suit-quality predicates so the authored surfaces match the intended "2 of top 3 or 3 of top 5" standard more closely.~~ Surface clauses encode the two-of-top-three or three-of-top-five predicate; leave only if a richer predicate shape is wanted later.
