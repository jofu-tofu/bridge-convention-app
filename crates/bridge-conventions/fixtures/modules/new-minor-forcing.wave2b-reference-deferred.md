# New Minor Forcing — Deferred Surface Work

- Replace the current quick-reference / response-table view with opener's actual NMF rebid ladder instead of the generic checkback summary.
- Raise the authored surface floor from `10+` toward the canonical `11+` treatment where the authority requires it.
- Add an explicit variant split for game-forcing 2-way NMF / XYZ styles if the product wants to teach those separately.
