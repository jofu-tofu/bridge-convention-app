[package]
name = "bridge-engine"
version = "0.1.0"
edition = "2021"

[features]
default = []
dds = ["dep:dds-bridge"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
rand_chacha = "0.3"
thiserror = "2"
dds-bridge = { version = "0.8", optional = true }
